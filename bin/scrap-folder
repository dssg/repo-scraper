#!/usr/bin/env python
from repo_scraper import scraper
import sys

folder_path = sys.argv[1] if len(sys.argv) > 1 else '.'
results = scraper.scrap_folder(folder_path)

for filename, result in results:
        if result[0]:
            print '%s result is %s\n' % (filename, result)